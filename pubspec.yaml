name: flutter_callouts
description: Point stuff out for your users => Target, Transform, Configure Callouts, Record to JSON in Dev, and Play in Producton
version: 0.0.3
maintainer: Ian White
homepage: https://github.com/biancashouse/flutter_callouts
# github mirrored from bitbucket
#homepage: https://bitbucket:org/biancashouse/flutter_callouts/src/master/
#issue_tracker: https://github.com/flutter/flutter/issues?q=is%3Aissue+is%3Aopen+label%3A%22p%3A+flutter_adaptive_scaffold%22
repository: https://github.com/biancashouse/flutter-callouts
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  logger: ^2.4.0
  pointer_interceptor: ^0.10.1+1
  gradient_borders: 1.0.1

  bh_shared:
    path: ../bh_shared

#dependency_overrides:
#  web: ^0.5.0 # latest version as of now

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.25.2
  mockito: ^5.4.4
#  build_runner: ^2.4.9
#  json_serializable: ^6.5.3
#  vector_math: ^2.1.2
#  text_editor: ^0.7.0
#  bloc_test: ^9.1.1
  flutter_lints: ^4.0.0
#  dart_mappable_builder: ^4.2.2
#  analyzer: ^6.4.1

#false_secrets:
#  - /example/lib/firebase_options.dart

# The following section is specific to Flutter packages.
flutter:

  uses-material-design: true

  assets:
    - lib/assets/
    - lib/assets/images/


